<?xml version="1.0" encoding="utf-8" ?>
<local:PageBase
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BrickController2.UI.Pages"
    xmlns:converters="clr-namespace:BrickController2.UI.Converters"
    xmlns:templateSelectors="clr-namespace:BrickController2.UI.DataTemplateSelectors"
    x:Class="BrickController2.UI.Pages.DeviceDetailsPage"
    Title="Device details">

    <local:PageBase.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <converters:DeviceTypeToSmallImageConverter x:Key="DeviceTypeToSmallImage"/>
            
            <!-- Data templates -->
            <DataTemplate x:Key="EptyOutputLevelSelectorTemplate">
                <ViewCell>
                </ViewCell>
            </DataTemplate>
            
            <DataTemplate x:Key="BuWizzOutputLevelSelectorTemplate">
                <Picker SelectedIndex="{Binding SelectedOutputLevelIndex}">
                    <Picker.Items>
                        <x:String>Low</x:String>
                        <x:String>Normal</x:String>
                        <x:String>High</x:String>
                    </Picker.Items>
                </Picker>     
            </DataTemplate>
            
            <DataTemplate x:Key="BuWizz2OutputLevelSelectorTemplate">
                <Picker SelectedIndex="{Binding SelectedOutputLevelIndex}">
                    <Picker.Items>
                        <x:String>Low</x:String>
                        <x:String>Normal</x:String>
                        <x:String>High</x:String>
                        <x:String>Ludicrous</x:String>
                    </Picker.Items>
                </Picker>
            </DataTemplate>
            
            <templateSelectors:DeviceOutputLevelDataTemplateSelector
                x:Key="DeviceOutputLevelDataTemplateSelector"
                EmptyOutputLevelSelectorTemplate="{StaticResource EptyOutputLevelSelectorTemplate}"
                BuWizzOutputLevelSelectorTemplate="{StaticResource BuWizzOutputLevelSelectorTemplate}"
                BuWizz2OutputLevelSelectorTemplate="{StaticResource BuWizz2OutputLevelSelectorTemplate}"/>
        </ResourceDictionary>
    </local:PageBase.Resources>

    <local:PageBase.ToolbarItems>
        <ToolbarItem Icon="ic_menu.png" Order="Primary" Command="{Binding MenuCommand}"/>
    </local:PageBase.ToolbarItems>

    <local:PageBase.Content>
        <Grid Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Device info -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="{Binding Device.DeviceType, Converter={StaticResource DeviceTypeToSmallImage}}" WidthRequest="50" HeightRequest="50" VerticalOptions="Start"/>

                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Column="0" Grid.Row="0" Text="Device name" FontSize="Small"/>
                    <Label Grid.Column="1" Grid.Row="0" Text="{Binding Device.Name}" FontSize="Medium" FontAttributes="Bold"/>

                    <Label Grid.Column="0" Grid.Row="1" Text="Device type" FontSize="Small"/>
                    <Label Grid.Column="1" Grid.Row="1" Text="{Binding Device.DeviceType}" FontSize="Medium" FontAttributes="Bold"/>

                    <Label Grid.Column="0" Grid.Row="2" Text="Address" FontSize="Small"/>
                    <Label Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2" Text="{Binding Device.Address}" FontSize="Micro"/>
                </Grid>
            </Grid>

            <!-- Outputs -->
            <StackLayout Grid.Row="1">
                <BoxView BackgroundColor="#E0E0E0" HeightRequest="1" VerticalOptions="Center" HorizontalOptions="Fill" Margin="0,8,0,8"/>
                <ListView ItemsSource="{Binding Outputs}" HasUnevenRows="True" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout>
                                    <Slider Value="{Binding Output}" Minimum="-255" Maximum="255" HeightRequest="40"/>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <BoxView BackgroundColor="#E0E0E0" HeightRequest="1" VerticalOptions="Center" HorizontalOptions="Fill" Margin="0,8,0,8"/>
            </StackLayout> 

            <!-- Connect -->
            <Frame Grid.Row="2" BackgroundColor="Green" HorizontalOptions="Fill" HeightRequest="40" WidthRequest="300" CornerRadius="10" Padding="0" Margin="0, 8, 0, 8">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ConnectCommand}"/>
                </Frame.GestureRecognizers>
                <Label Text="Connect" TextColor="White" FontSize="Medium" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Frame>
            
        </Grid>
    </local:PageBase.Content>
</local:PageBase>