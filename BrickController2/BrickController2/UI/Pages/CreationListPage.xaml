<?xml version="1.0" encoding="utf-8" ?>
<local:PageBase
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BrickController2.UI.Pages"
    xmlns:behaviors="clr-namespace:BrickController2.UI.Behaviors"
    x:Class="BrickController2.UI.Pages.CreationListPage"
    Title="Creation list">

    <local:PageBase.ToolbarItems>
        <ToolbarItem Name="Add" Command="{Binding AddCreationCommand}"/>
        <ToolbarItem Name="Menu" Command="{Binding MenuCommand}"/>
    </local:PageBase.ToolbarItems>
    
    <local:PageBase.Content>
        <StackLayout>

            <ListView ItemsSource="{Binding Creations}" SelectionMode="None" HasUnevenRows="True">
                <ListView.Behaviors>
                    <behaviors:ListViewItemTappedBehavior Command="{Binding CreationTappedCommand}"/>
                </ListView.Behaviors>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" Padding="10">
                                <Label Text="{Binding Name}" FontSize="Large" FontAttributes="Bold"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
        </StackLayout>
    </local:PageBase.Content>
</local:PageBase>